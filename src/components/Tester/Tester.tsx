import classes from "./Tester.module.scss";
const Tester = () => {
  return (
    <p className={classes.paragraph}>
      this is some component to test the scss modules
    </p>
  );
};

export default Tester;
