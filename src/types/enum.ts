export enum StudenStatus {
  NEW,
  PROGRESS,
  DISCONNECTED,
  FINISHED,
}
export enum QuestionType {
  MULTICHOICE,
  PARAGRAPH,
}

export enum QuestionStatus {
  NOT_VIEWED,
  VIEWED,
  SKIPPED,
  ANSWERED,
}
