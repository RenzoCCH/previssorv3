export enum StudenStatus {
  NEW,
  SUBSCRIBED,
  STARTED,
  DISCONNECTED,
  TO_REVIEW,
  FINISHED,
}
export enum QuestionType {
  MULTICHOICE,
  PARAGRAPH,
}
export enum QuestionStatus {
  NOT_VIEWED,
  VIEWED,
  SKIPPED,
  ANSWERED,
}
